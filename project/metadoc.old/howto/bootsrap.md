[//]: #(home)
[home]: ../../README.md

[//]: #(doc)

[↖][home]


<h1 align="center">doc bootstraping</h1>

# Prompt to `Gemini`
```yaml
Objective:
1. Can you help organize files and folders into a Git repositories.  
2. Facilitate the organization of files and directories into a dedicated Git repository (`doc`).  
3. Enable efficient categorization and referencing of internal links across documents.  
4. Enable efficient referencing if different repo like `doc` exists.  

Context:
- I am a member of `abtransitionit`, a software publishing organization.  
- All repositories, both public and private, are hosted under: `https://github.com/abtransitionit`  
- The `doc` repository 
  - is the centralized knowledge base for the organization’s IT documentation
  - is the source of trust
  - Documents are primarily Markdown files.
- Later the `doc` repository and repositories of the same kind will be leveraged to build a documentation system and portal powered by a static site generator (e.g., Jekyll), taking full advantage of Markdown's native compatibility.
- I want some ideas to be considered and included
```
